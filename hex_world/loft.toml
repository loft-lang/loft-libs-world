[package]
name    = "hex_world"
version = "0.2.0"
loft    = ">=2026.7.2"

api_compatible_with  = "0.1.2"   # oldest own release this is a drop-in for
data_compatible_with = "0.1.2"   # oldest own release whose stored data this reads
[library]
entry = "src/hex_world.loft"
